import React from "react";

const Listing = (props) => {
    const {
        records = [],
        columns = [],
    } = props;

    return <div className={`listing`}>

    </div>
}

export default Listing;